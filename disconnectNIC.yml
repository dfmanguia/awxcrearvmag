---
- name: Changing network adapter
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name:  Changing network adapter
      vmware_guest_network:
        name: "{{ vm_name }}"
        networks:
        - name: "{{ vm_net_namemgmt }}"
          connected: False

        
