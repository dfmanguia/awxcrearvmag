---
- name: Changing network adapter
  hosts: localhost
  tasks:
    - name:  Changing network adapter
      community.vmware.vmware_guest:
        validate_certs: false  
        name: "{{ vm_name }}"  
        folder: "FolderName"
        network_name: "{{ vm_net_namemgmt }}"
        connected: false
